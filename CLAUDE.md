# 项目规则

## 操作权限

- **查询类操作**（读取文件、搜索代码、查看 git 状态/日志/差异、列出目录、grep 等只读操作）无需询问，直接执行。

## 命令行

- 执行终端命令时优先使用 PowerShell，其次才用 bash。

## 路径规范

- 记录路径时一律使用动态/相对地址（如相对项目根的 `Assets/...`、`.claude/...`），禁止写死项目绝对路径（如 `e:\Unity\Project\IL\IL\...`）。这同样适用于记忆文件、文档与生成的代码，避免换盘符或换机器后失效。

## 任务总结

- 如果任务中调用了 Agent 或 Skill，必须在任务总结时列出：具体调用了哪些 Agent/Skill、每次调用执行了什么任务。
- 每次任务总结完成时，都必须明确汇报本次任务是否调用了对应的 Agent 或 Skill（即使没有调用，也要说明"未调用任何 Agent/Skill"）。
