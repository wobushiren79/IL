# 路径规范：用相对地址

记录/书写路径时一律使用动态/相对地址（相对项目根，如 `Assets/...`、`.claude/...`），禁止写死项目绝对路径（如 `e:\Unity\Project\IL\IL\...`）。适用于记忆文件、文档、生成的代码。

**Why:** 绝对路径在换盘符或换机器后会失效，且团队成员的项目根路径各不相同。

**How to apply:** 任何要落盘的路径先转成相对项目根的写法。该规则同样写在项目根 `CLAUDE.md` 的「路径规范」一节。
